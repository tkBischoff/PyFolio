pipeline {
    agent any

    stages {
        stage("verify tooling") {
            steps {
                withSonarQubeEnv(installationName: 'sq1') {
                  sh "sonar-scanner --version"
                }
            }
        }
    }
}
